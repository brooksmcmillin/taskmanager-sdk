name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 2 * * 1'
  workflow_dispatch:

jobs:
  security:
    uses: brooksmcmillin/workflows/.github/workflows/python-security.yml@main
    with:
      package-name: taskmanager_sdk
